# Include inside the existing game.elopros.com HTTPS server.
location = /gates/watch {
    return 308 /gates/watch/;
}
location ^~ /gates/watch/ {
    proxy_pass http://127.0.0.1:8081/;
    proxy_read_timeout 5s;
    proxy_connect_timeout 2s;
    proxy_buffering off;
    proxy_cache off;
}
