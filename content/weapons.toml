# Gates · content/weapons.toml — combat rows (CONTENT.md §1). Damage is
# per body hit; TTK bands in balance.toml assert hits-to-kill (CONTENT §4
# anchor 2). `ballistic` present = projectile (M2 consumes speed/drop);
# absent on a firearm = hitscan.
#
# `structure` is the second damage column: what one hit takes off a
# building piece or a deployable. It is its own number, never `damage`
# scaled — a spear that kills a man in four hits must not open a wall in
# four. Two laws hold it (balance.rs, anchor 1): no weapon's `structure`
# may exceed its own `damage`, and the throwable raid tool's must strictly
# exceed every melee weapon's — the raid tool stays the raid tool. The
# raid-ratio anchor divides wall hp by the satchel's `structure`.

# --- tools that are also weapons ---------------------------------------
#
# **A hatchet did no damage to anything until 2026-08-08, and that was a
# hole rather than a design.** `weapons.toml` armed six things, none of them
# a tool, so `held_melee` returned `None` for every pocket the spawn kit
# fills — a fresh character could chop a tree and could not hit a pig, a
# player, or a door with the same axe. `crates/server/tests/hunt.rs` found
# it by trying to hunt with what a spawn actually has.
#
# **The window is not a taste call.** `[bands] ttk_melee = [3, 5]` against
# `player_hp = 100` means every `kind = "melee"` row must do 20–34 damage or
# the content refuses to load, so a tool cannot be a *feeble* weapon here —
# it can only sit at the bottom of the same band a spear sits in. That is
# the honest cost of the band as written, and whether it should widen so a
# rock is meaningfully worse than a spear is a knob, not a loop's call
# (`DECISIONS.md` §open, "tools as weapons").
#
# What separates them instead is **reach**: 1 m against the spears' 2 m, so
# a spear wins the fight it starts. Gather reach is `gather::REACH_M` and is
# untouched — swinging at a tree is not affected by this table.
#
# `structure` stays at or below the metal spear's 4 on every row, so the
# best-melee anchor (`balance.rs`, the door and wall breach swings) still
# resolves to `item.spear_metal` and no raid number moves.

[[weapon]]
id = "item.rock"
kind = "melee"
damage = 20
structure = 1
headshot_mult = 2
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.hatchet_stone"
kind = "melee"
damage = 25
structure = 2
headshot_mult = 2
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.pickaxe_stone"
kind = "melee"
damage = 25
structure = 1
headshot_mult = 2
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.hatchet_metal"
kind = "melee"
damage = 30
structure = 3
headshot_mult = 2
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.pickaxe_metal"
kind = "melee"
damage = 30
structure = 2
headshot_mult = 2
rate_per_min = 48
range_m = 1

# --- weapons proper ------------------------------------------------------

[[weapon]]
id = "item.spear_wood"
kind = "melee"
damage = 20
structure = 3
headshot_mult = 2
rate_per_min = 48
range_m = 2

[[weapon]]
id = "item.spear_metal"
kind = "melee"
damage = 30
structure = 4
headshot_mult = 2
rate_per_min = 48
range_m = 2

[[weapon]]
id = "item.bow"
kind = "bow"
damage = 30
structure = 1
headshot_mult = 2
rate_per_min = 30
range_m = 60
ammo = "item.arrow_wood"

[weapon.ballistic]
speed_mps = 40
drop_mps2 = 20

[[weapon]]
id = "item.crossbow"
kind = "bow"
damage = 35
structure = 1
headshot_mult = 2
rate_per_min = 20
range_m = 80
ammo = "item.arrow_metal"

[weapon.ballistic]
speed_mps = 55
drop_mps2 = 20

[[weapon]]
id = "item.revolver"
kind = "firearm"
damage = 20
structure = 1
headshot_mult = 2
rate_per_min = 150
range_m = 50
ammo = "item.pistol_ammo"

[[weapon]]
id = "item.satchel_charge"
kind = "throwable"
damage = 475
structure = 125
headshot_mult = 1
rate_per_min = 10
range_m = 10
# Seconds between planting the charge and the blast. The only field that
# is the throwable's alone: every other column above is shared with the
# melee rows, and a fuse has no meaning on a swing, so `validate` requires
# it here and refuses it everywhere else. It is what makes the raid a
# commitment rather than a click — you are standing at the wall you are
# breaking, and the defender gets the same seconds you do.
# Proposed default, DECISIONS.md §open ("satchel fuse v0").
fuse_s = 10
# Blast radius in metres — one build cell, so the hole a charge is INTENDED
# to clear is the cell it was planted on: the wall you stuck it to, the
# floor under that wall, and whatever shares the cell's corner.
#
# NOTHING READS THIS YET. The column is baked, validated and hashed, and no
# sim code consumes the result: `charge.rs` has `place` and `tick_fuses`
# and no falloff. So a charge today still damages the one address it was
# planted on, exactly as `satchel fuse v0` says it does, and `balance.rs`
# anchor 1 still divides wall hp by this row's 500 undisturbed. The
# intended curve — both damage columns falling linearly to zero at this
# distance, `structure` whole at the planted address because the distance
# there is zero, and `damage` becoming what the blast takes off a player
# standing in it — is the NEXT slice's, not this one's.
# Proposed default, DECISIONS.md §open ("satchel blast v0").
blast_m = 3
