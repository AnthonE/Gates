# Gates · content/weapons.toml — combat rows (CONTENT.md §1). Damage is
# per body hit; TTK bands in balance.toml assert hits-to-kill (CONTENT §4
# anchor 2). A `kind = "bow"` row is a projectile weapon and its rounds
# carry the ballistics (the `[[ammo]]` table at the foot of this file); a
# firearm names rounds with no `[[ammo]]` row and is hitscan.
#
# `structure` is the second damage column: what one hit takes off a
# building piece or a deployable. It is its own number, never `damage`
# scaled — a spear that kills a man in four hits must not open a wall in
# four. Two laws hold it (balance.rs, anchor 1): no weapon's `structure`
# may exceed its own `damage`, and the throwable raid tool's must strictly
# exceed every melee weapon's — the raid tool stays the raid tool. The
# raid-ratio anchor divides wall hp by the satchel's `structure`.
#
# `headshot_mult` and `limb_pct` are the two ends of the body-part ladder
# (`reference/PROJECTILES.md` §0: head ×2, chest ×1, limbs ×0.5, with a
# per-weapon override). The head is a multiplier and the limb a **percent**
# because one integer column cannot say both "double" and "half"; 100 and 1
# respectively are the identities, and the satchel carries both because a
# blast has no anatomy to aim at. `[bands]` pins each to exactly one value
# on every non-throwable row, so the ladder is a shape rather than a per-row
# taste call until a weapon earns an override — which is what the columns
# are for and why they are per-row and not two globals.
#
# **Only `bow` and `firearm` rows reach the sim** (`bake_ranged`). The melee
# rows carry both columns priced for a mechanic that does not exist: a swing
# is resolved feet-to-feet in a plane, so there is no height to test
# (`combat::strike`, and `RangedDef::headshot_mult`'s doc says it in full).

# --- tools that are also weapons ---------------------------------------
#
# **A hatchet did no damage to anything until 2026-08-08, and that was a
# hole rather than a design.** `weapons.toml` armed six things, none of them
# a tool, so `held_melee` returned `None` for every pocket the spawn kit
# fills — a fresh character could chop a tree and could not hit a pig, a
# player, or a door with the same axe. `crates/server/tests/hunt.rs` found
# it by trying to hunt with what a spawn actually has.
#
# **The window is not a taste call.** `[bands] ttk_melee = [3, 5]` against
# `player_hp = 100` means every `kind = "melee"` row must do 20–34 damage or
# the content refuses to load, so a tool cannot be a *feeble* weapon here —
# it can only sit at the bottom of the same band a spear sits in. That is
# the honest cost of the band as written, and whether it should widen so a
# rock is meaningfully worse than a spear is a knob, not a loop's call
# (`DECISIONS.md` §open, "tools as weapons").
#
# What separates them instead is **reach**: 1 m against the spears' 2 m, so
# a spear wins the fight it starts. Gather reach is `gather::REACH_M` and is
# untouched — swinging at a tree is not affected by this table.
#
# `structure` stays at or below the metal spear's 4 on every row, so the
# best-melee anchor (`balance.rs`, the door and wall breach swings) still
# resolves to `item.spear_metal` and no raid number moves.

[[weapon]]
id = "item.rock"
kind = "melee"
damage = 20
structure = 1
headshot_mult = 2
limb_pct = 50
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.hatchet_stone"
kind = "melee"
damage = 25
structure = 2
headshot_mult = 2
limb_pct = 50
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.pickaxe_stone"
kind = "melee"
damage = 25
structure = 1
headshot_mult = 2
limb_pct = 50
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.hatchet_metal"
kind = "melee"
damage = 30
structure = 3
headshot_mult = 2
limb_pct = 50
rate_per_min = 48
range_m = 1

[[weapon]]
id = "item.pickaxe_metal"
kind = "melee"
damage = 30
structure = 2
headshot_mult = 2
limb_pct = 50
rate_per_min = 48
range_m = 1

# --- weapons proper ------------------------------------------------------

[[weapon]]
id = "item.spear_wood"
kind = "melee"
damage = 20
structure = 3
headshot_mult = 2
limb_pct = 50
rate_per_min = 48
range_m = 2

[[weapon]]
id = "item.spear_metal"
kind = "melee"
damage = 30
structure = 4
headshot_mult = 2
limb_pct = 50
rate_per_min = 48
range_m = 2

[[weapon]]
id = "item.bow"
kind = "bow"
damage = 30
structure = 1
headshot_mult = 2
limb_pct = 50
rate_per_min = 30
range_m = 60
ammo = ["item.arrow_wood"]

[[weapon]]
id = "item.crossbow"
kind = "bow"
damage = 35
structure = 1
headshot_mult = 2
limb_pct = 50
rate_per_min = 20
range_m = 80
ammo = ["item.arrow_metal"]

[[weapon]]
id = "item.revolver"
kind = "firearm"
damage = 20
structure = 1
headshot_mult = 2
limb_pct = 50
rate_per_min = 150
range_m = 50
ammo = ["item.pistol_ammo"]
# Eight rounds and 3.4 s, both the reference's own published figures for
# this weapon, taken rather than argued (`reference/BALANCE.md` §6: take
# theirs, a case is needed only to differ). The magazine is what gives a
# firefight a rhythm — without it `rate_per_min` alone makes a gun an
# unbroken stream of clicks and the body-part ladder above is only a
# slightly longer stream of them.
#
# Neither bow carries one, and that IS a difference from the reference,
# which models a bow as a one-round magazine reloaded between shots (a bow
# is a `BaseProjectile` like every gun — `reference/PROJECTILES.md` §1).
# The mechanism is the case: a one-round magazine reloaded after every shot
# is the nock, and the nock is already `rate_per_min` here, so carrying
# both would be two cadences to keep in step for one wait.
magazine = 8
reload_ms = 3400

[[weapon]]
id = "item.satchel_charge"
kind = "throwable"
damage = 475
structure = 125
headshot_mult = 1
limb_pct = 100
rate_per_min = 10
range_m = 10
# Seconds between planting the charge and the blast. The only field that
# is the throwable's alone: every other column above is shared with the
# melee rows, and a fuse has no meaning on a swing, so `validate` requires
# it here and refuses it everywhere else. It is what makes the raid a
# commitment rather than a click — you are standing at the wall you are
# breaking, and the defender gets the same seconds you do.
# Proposed default, DECISIONS.md §open ("satchel fuse v0").
fuse_s = 10
# Blast radius in metres — one build cell, so the hole a charge is INTENDED
# to clear is the cell it was planted on: the wall you stuck it to, the
# floor under that wall, and whatever shares the cell's corner.
#
# READ BY `charge::detonate` since satchel blast v0. This line said
# "NOTHING READS THIS YET" through the whole slice that made it load-
# bearing, which is the drift `CLAUDE.md` warns about: a comment claiming
# a column is inert reads as permission to change it freely.
#
# The curve it describes is the one that shipped: both damage columns fall
# linearly to zero at this distance, `structure` lands whole at the planted
# address because the distance there is zero, and `damage` is what the
# blast takes off a body standing in it. The scan is the 3×3 column ring
# around the epicentre, and `charge.rs`'s const block asserts this radius
# cannot outrun that ring — so widening `blast_m` past one build cell is a
# sim change, not a content change.
# Proposed default, DECISIONS.md §open ("satchel blast v0").
blast_m = 3

# --- the rounds ------------------------------------------------------------
#
# **Ballistics belong to the round, not to the bow that fires it**
# (`reference/PROJECTILES.md` §9.3; operator, 2026-08-10). These two blocks
# were `[weapon.ballistic]` on the bow and the crossbow until the schema
# moved, and the numbers are unchanged in the move — `item.arrow_wood` is
# the bow's old 40 m/s and `item.arrow_metal` the crossbow's old 55, so no
# arrow shipped today flies differently than it did before.
#
# The reference game hangs these off `ItemModProjectile`, a mod on the ammo
# item, which is how one bow there fires wooden, bone, high-velocity and
# fire arrows that differ in speed, drop and impact. With the numbers on the
# weapon that is unreachable at any values: the bow decides how fast its
# arrow flies, so every arrow it fires flies the same.
#
# What this table does NOT yet carry is a damage column. Theirs scales the
# weapon's damage by the round's (a high-velocity arrow is −20 %), and that
# multiplier is a number nobody has spoken — `DECISIONS.md` §open, not this
# file. So damage stays on the weapon and every round out of one bow hits
# equally hard, which is the honest half-way house and is stated rather
# than hidden.
#
# `speed_mps` has a hard ceiling the bake enforces and it is not a taste
# call: an arrow faster than `ARROW_STEP_MM × MAX_ARROW_SUBSTEPS` per tick
# (2.72 m) passes through a wall between two collision samples. Content
# that fast is refused at boot rather than clamped.

[[ammo]]
id = "item.arrow_wood"
speed_mps = 40
drop_mps2 = 20

[[ammo]]
id = "item.arrow_metal"
speed_mps = 55
drop_mps2 = 20
