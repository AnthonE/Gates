# Gates · DECISIONS.md — spoken, and waiting to be spoken

Two lists. **Spoken** is append-only: date, the call, the operator's own
words, a pointer. **Open** is the knob registry — every **(knob)** in the
docs, deduped, with its shipping default. A loop may rely on a default;
only the operator moves a knob from open to spoken. No number gets
invented into code that isn't on this page.

## Spoken

| date | decision | pointer |
|---|---|---|
| 2026-07-30 | **The game is a separate repo, sold through scry's Great Work board by the operator's own agent** (*"ill make it a separate repo… make sure the website and backend are all setup for this"*). Quest `munus-first-sale` posted; claim/submit verbs live on the board. | scry `SENTENCES.md` · `MUNUS.md` |
| 2026-07-30 | **The stack**: Rust server, three.js client, WebTransport/QUIC, no-blocking/no-alloc server discipline (*"backend is in rust, frontend three.js… no allocations in the hotpath… it should be a great game to build upon if we ace the skeleton"*). | `DESIGN.md` · `NETCODE.md` |
| 2026-07-30 | **The coins**: OBOL is the in-world working coin; SCRY and MYRRH price skins, cosmetic only (*"how might we manage to get obol into the mix? Scry and Myrhh can be the premium currency for skins"*). Never-table stands as a wall. | `DESIGN.md` §3 |
| 2026-07-30 | **Built by a loop system** (*"we are going to use a loop system we have to build this out"*). Loop discipline in `CLAUDE.md`; this file + `NOW.md` are the steering. | `CLAUDE.md` |
| 2026-07-31 | **The name is `Gates`** — not `ashfall`, which no one chose (*"idk where the name ashfall came from its 'Gates' or Scry Gate"*; asked which, answered `Gates`). Matches the repo; stays a separate brand orbiting scry per DESIGN §13. Knob closed, removed from §open and DESIGN §14. | `DESIGN.md` §13 |
| 2026-07-31 | **Art direction: "Rust with a darker edge"** (*"its rust with a darker edge"*). Genre legibility and information design stay; the register is colder — threshold and funerary rather than sun-bleached scavenger-industrial, which the naming already carries (OBOL is the coin that pays the ferryman, MYRRH is embalming resin, the game is *Gates*). The checked-in reference images are a **craft** bar — material read, HUD legibility, terrain depth, human scale — not a style to reproduce. **IP posture, same call** (*"games are games"*, *"avoid names"*, *"lets not be too harsh"*): the rail is exactly two things — no Facepunch proper nouns, and no asset copied verbatim. Genre convention (pine forest, hotbar, vitals stack, upkeep, wipes, extraction) is nobody's property; no pass may refuse or narrow work on IP grounds beyond those two. Concrete palette, material set, and silhouette language stay **§open** until spoken. | `gates-loop/art/RUBRIC.md` |
| 2026-08-01 | **Reference the genre before inventing** (*"we need to reference the rust game with stuff like this"*). When a design question comes up — spawn placement, respawn flow, loot pacing, a UI affordance — look at how the genre already solved it before designing from scratch. The checked-in `Rust Images/` are one instance of this, not the whole of it. Caught concretely: fresh spawns were landing inside trees because the shipped placeholder picked forest interior, while `TERRAIN.md` §1 and `DESIGN.md` §2 both already said **beach**. The answer was written down before anyone invented a worse one. | `TERRAIN.md` §1 · `DESIGN.md` §2 |

## Open (defaults ship until spoken)

| knob | default | doc |
|---|---|---|
| shard cap / reference box | 100 / 4-core VPS | DESIGN §14 |
| wipe cadence · BP survival | monthly · BPs one extra cycle | DESIGN §2 |
| island size | 2,048 m | TERRAIN §6 |
| hunger depth | minimal timer-drain | DESIGN §2 |
| day/night length | 45 min | ALPHA §1 |
| bag cooldown · cap | 5 min · 8 | ALPHA §1 |
| nametag range | 8 m, aim-only | ALPHA §1 |
| local chat | on, 20 m | ALPHA §1 |
| claim grace (disconnect standing window) | 10 s | NETCODE §6.3 |
| haven sleeper timeout | 20 min | NETCODE §6.3 |
| node/barrel respawn | 20–45 / 15–30 min jittered | TERRAIN §2 · CONTENT §5 |
| despawn base constant | 5 min × rarity (≈5/20/40/60) | CONTENT §1 |
| balance bands: raid ratio · TTK · farm rate | ≈1.5× starter · 3–6 hits · 300/node — declared as data in `content/balance.toml` v0 (raid stone 1.0–3.0× rising by tier · TTK melee 3–5 / bow 3–4 / revolver 4–6 / headshot ×2 exactly / armor +≤2 hits · node 250–400 over 8–12 hits · wood wall 5–9 min · upkeep ≤15 min/day), computed and asserted by `test_content`, refused at boot | CONTENT §4 |
| player max hp (TTK divides by it) | 100 | CONTENT §4 · content/balance.toml |
| content opening values (alpha set v0) | `content/*.toml` as committed — ~48 items, every number a knob with the file as its default; farm rates 50/50/30/30/20 per min, components 4 road-min (fat 2), satchel 240 gunpowder | CONTENT §2 · content/ |
| content dir | `content/` at repo root; `shard.toml content_dir` overrides | CONTENT §0 · server `config.rs` |
| revolver: barrel drop or craft-only | rare drop | CONTENT §5 |
| food set (meat cut?) | berries/mushrooms/corn, meat cut | CONTENT §2 |
| bank deposit fee | 2%, burns | DESIGN §3.1 |
| OBOL allotment + claim cadence | unset — scry-side operator act | DESIGN §3.1 |
| skin catalog, prices, SCRY/MYRRH split | unpriced until posted | DESIGN §3.2 |
| skin proceeds fiscus/burn split | 100% fiscus | DESIGN §3.2 |
| queue priority for sale | **never** (flipping is a sentence) | DESIGN §3.3 |
| A1→A2→A3 arming dates | unset — each an operator act | ALPHA §2 |
| WebSocket fallback lane | not in alpha | NETCODE §2 |
| hosting provider · domain | unset | ALPHA §3 |
| playtest channel (Discord/Telegram) | unset | ALPHA §4 |
| coalescing rates · stream-in batch sizes | per NETCODE, bench-tuned | NETCODE §5 |
| grass · tree LOD distances · map-as-item | cosmetic defaults | TERRAIN §4/§5 |
| sim movement constants | walk 3.0 · sprint 5.5 m/s · gravity 20 m/s² · terminal 50 m/s · wade ×0.5 below ground 0.4 m · world border margin 8 m | sim-core `movement.rs` |
| velocity quantization | 1 cm/s (positions per DESIGN §5.5: 3 cm x/z · 1 cm y) | sim-core `movement.rs` |
| worldgen shape params | **all** shape constants in `terrain.rs` (relief 1/600 m gain 2.4 · warp 45 m @ 1/1200 · coast r 960±100 edge 160 · sea floor 12 m · moisture 1/700 · ridge 16 m @ 1/220 from 52 m · biome bands beach <2 m / highland >52 m / forest moist >0.05 · remap LUT v0 · scatter weights v0), pinned by `test_terrain_golden` | sim-core `terrain.rs` |
| command buffer cap | 256 per tick, overflow defers to next tick | sim-core `limits.rs` |
| beach spawn ring | the spawn zone is the **beach**, as `TERRAIN.md` §1 stage 6 and `DESIGN.md` §2 already said; the retired placeholder sampled the interior 224–1824 m, which *is* the forest band, so fresh spawns stood inside scatter · 48 hashed candidates, each one bearing off the shared 256-entry yaw LUT (no trig, wall 1) plus a 12-step bisection along that ray for the shoreline crossing at **1.2 m** — above the 0.4 m wade line so a spawn is on sand, below the 2 m beach mask so it is beach and not shelf · bracket 640–1024 m from island center: land is solid inside 640 and the sea floor is under the target before 1024, which is also the largest radius keeping every bearing's outer probe inside the 2048 m square · accept = under the beach mask **and** slope < 1.0 (the foundation/walkability shape) **and** 4 m clear of every scatter slot center — the widest archetype the client draws is the tree cone at r 1.7 m × 1.1 max scale ≈ 1.9 m, plus the 0.4 m capsule, so 4 m stands clear of one rather than merely outside it · the clearance scan covers the 3×3 cell block, conservative for any clearance under 9 m (a slot two cells out is ≥ 16 − 4 − 3 m away) · two fallbacks, both gated unreachable: the first merely-walkable shore point, then island center · measured on the way in: the worst spawn over 400 seeds × 64 ids consumed 7 of the 48 candidates, so nothing is near a fallback · `spawn_ring_lands_on_a_clear_beach` re-derives beach/wade/slope/clearance from terrain over 32 seeds × 64 joins and scans 5×5 cells where the selector scans 3×3, so a clearance that outgrew the scanned block reddens instead of passing · **`dev_spawn` still overrides everything** (row below) | sim-core `world.rs` · `terrain.rs` |
| snapshot v0 wire widths | pos x/z 17 bit · y 14 bit @ −20.48 m bias · vy 14 bit @ ±81.9 m/s (covers terminal 50; NETCODE §3's ±16 predates the spoken terminal) · pos-delta 8/10/8 bit · ids u32 · counts 7 bit, pinned by `test_protocol_golden` | protocol `lib.rs` |
| server ring & buffer caps | input ring 32 datagrams (drop newest — redundancy re-carries) · snapshot ring 4 (skip) · ctrl 8 / graveyard 256 (refuse/retry) · input buffer 16 frames (skip ahead) · pending removals 256 (resync) | sim-core `limits.rs` |
| nudge fill-ins (NETCODE §4 speaks: depth target 1–2 · consume 0/1/2 · hard-resync on empty or >6) | consume 2 above depth 6 · Faster at 0 · Ok 1–2 · Slower ≥3 · 30 starved-tick debounce before HardResync | server `client.rs` · sim-core `limits.rs` |
| snapshot fill overflow streak | 3 consecutive byte-overflow refusals end the fill loop (bounded per-snapshot work, not a wire number) | server `core.rs` |
| net plumbing bounds | handshake timeout 5 s · writer poll 2 ms · sim backlog drop past 8 ticks | server `net.rs` |
| client netcode fill-ins (NETCODE §3/§4 speak dilation ~5% · smoothing 0.95/0.85 · interp 133 ms) | resync run-ahead 3 ticks · catch-up cap 4 steps/frame · server-est gain 0.1, snap past 6 ticks · error hard-snap 4 m · smoothing blend 0.25–1 m, dead zone 1 cm, per render frame · interp history 16 samples · buffer-dry ⇒ freeze (Hermite/extrapolation await wire velocity, M2) · input datagrams at 30 Hz (per client tick, not per render frame) · predictor ring 32 seqs · bridge in-buf 2 KiB · advance dt clamp 1 s | client-wasm `predict.rs` `interp.rs` `clock.rs` `core.rs` `bridge.rs` |
| client render cosmetics | eye 1.6 m · near ring 5×5 64 m chunks @1 m, one build in flight, one teardown/frame · far mesh 8 m grid at −0.15 m · terrain surface, every material response and the scatter's colour variation moved to the **materials v0** row below · fog and every light value moved to the **lighting v0** row below · scatter pool 4096/archetype · FOV 75, far 1500 m, pixel ratio ≤ 2 · mouse 0.0022 rad/px, pitch stop 0.02 rad · HUD 250 ms · datagram send pool 4 × 1200 B · gather toast fade 1.6 s, ≤ 8 on screen · hotbar 6 cells, DOM-diffed on the HUD timer, selected cell highlighted (keys 1–6) · weak-mark glint: unlit 0.18 m octahedron at the marked node, tree ring r 0.9 / lift 1.4, other nodes r 1.05 / lift 0.6 · build-piece render: slab 0.3 m · 0.9 m lintel · foundation lift 0.3 m, wall 0.24 m thick, doorway 0.9 m posts — these three are sim collision truth now, the renderer mirrors `collide.rs` (piece collision v0 row) · tier colors wood/stone/metal 0x8a6a45/0x84837c/0x5f6a72 (their PBR responses in the materials v0 row) · ghost green wireframe · build aim point 3.5 m ahead of the feet (the authoritative 5 m reach is server-side, build grid v0 row) · deploy stand-ins: per-archetype colored boxes (`scene.js` DEPLOY_STYLE; the door a 0.9 × 2.1 m panel in its doorway, swinging a quarter turn onto its hinge when open) | web `src/*` |
| dev spawn override | unset (shipping default: scattered spawns); `shard.toml dev_spawn = "x,z"` meters pins every join to one point — dev shards and the browser smoke only, never public; pins into the WAL header beside the seed when the WAL file lands | server `config.rs` · sim-core `world.rs` |
| dev view hook + welcome `dev` bit (wire v7) | a shard states what it is: `PROTO_VER` 7, goldens regenerated — the welcome grew a `dev` bit, true iff the shard runs a dev override (today exactly `shard.toml dev_spawn`), and the client installs its dev affordances on that bit alone, so a public shard's page never grows one · v0 affordance: `globalThis.__gatesDebug.setView(yaw, pitch)` sets `InputTracker`'s yaw/pitch directly (same pitch clamp as the mouse path, non-finite args refused), bound once and published on the existing 250 ms HUD timer's debug object — never the RAF path · `view: [yaw, pitch]` joins that snapshot ungated (a readout, not an affordance) · **why:** headless Chromium grants pointer lock but yields no `movementX`, measured, so a capture harness can walk and cannot look, and comparable fixed vantages are the whole point of the visual pass · `setPos` (free camera) is NOT in v0 — a base must still be walked to · consequence to know: dev-ness and pinned spawns are one key today, so a hand-run local shard wanting an aimable camera must also collapse every spawn to one point; a separate `dev = true` key would decouple them if the art loop ever needs scattered spawns · gated both ways by `ci/browser_smoke.mjs` (dev shard: hook present, aims the camera AND the sim; public-config shard: `dev=false`, no `setView`) and by `test_welcome_dev_bit_tracks_dev_spawn` | protocol `lib.rs` · server `net.rs` · client-wasm `bridge.rs` · web `src/input.js` `src/main.js` |
| gather verb v0 | swing every 38 ticks (~47/min, the melee rows' cadence) · reach 2.0 m (the melee rows' range) · planar aim cone half-angle 30° (cos √3/2 authored offline) · vertical window ±3 m, pitch ignored until M2's raycasts · point-blank ≤ 0.2 m bypasses the cone · cooldown paid per swing, hit or whiff · held item = the selected hotbar slot (wire `sel`, keys 1–6; an invalid non-wire selector falls back to slot 0); an item with no tool row swings as the hand row | sim-core `gather.rs` |
| weak spot v0 (the glint / the X) | per-player mark, appears after the first landed hit on a node · heading = hash(seed · cell · pid · landed-hit count) over the shared 256-heading yaw LUT, moves every landed hit · a hit landed standing inside the mark's sector pays yield × (100 + `weak_spot_bonus_pct`)/100, the pct per archetype in `content/gatherables.toml` (bush 0 = no mark) · sector half-angle 45° (cos √2/2 authored offline) · point-blank never bonuses (no bearing to judge) · chase resets on node switch or exhaust; exhaust announces no mark | sim-core `gather.rs` · content/gatherables.toml |
| gather bounds & overflow | item defs cap 64 (alpha set ~48; bake refuses past it) · tool rows per node 8 (alpha data uses ≤ 4 + hand; bake refuses past it) · inventory 6 + 24 slots (ALPHA §1) · slot-life store 16 384 entries (past the ~8–12 k live slots a seed produces): standing damage evicts lowest-hits-first (the evicted node heals), harvested entries never evicted · sim event ring 256/tick, drop newest, counted · yield an inventory can't hold is lost until ground drops land with the death/backpack slice | sim-core `limits.rs` `gather.rs` |
| browser smoke gate params | join timeout 60 s · play window 6 s · mutual-move floor 2 m (walk is 3 m/s, so a held key covers ~18 m; 2 survives shared-box load, fails a frozen remote) · two-tab spawn 1024,1024 @ seed 20260731, walkability guarded natively in sim-core `world::tests` · harness mechanics, not tunables: http 8934 / dev wire 4433 / public-config wire 4434 — the second shard exists only so the dev gate has a negative case (all env-overridable) · 30 s shard-boot wait · 250 ms polls · 1280×720 viewport · dev-hook aim yaw π/2 (faces +X, so a held W must walk east-dominant) / pitch −0.3 (clear of the clamp, so the clamp is not what is measured) / readback tolerance 1e-3 | `ci/browser_smoke.mjs` |
| gather wire v1 (the event lane) | S→C events ride the handshake bidi stream as `KIND_EVENT` subtypes (wire v1, goldens regenerated): gather toast · own-inv slot diff vs the last-queued shadow · slot harvested/respawned broadcast · harvested-set join sync (batch 64 cells · scan 256 entries/tick · reset bit; a mid-walk swap-remove can ghost a node client-side until its own respawn — bounded by the respawn window, accepted over cursor machinery) · item-name catalog (batch 8 · name ≤ 24 B, bake refuses longer) · msg cap 320 B · per-conn event ring 64, refusal ⇒ `ev_resync` (walk + catalog restart; inv shadow re-diffs) · ≤ 1 msg per drip kind per client per tick · client toast ring 8, drop oldest · toasts only for added > 0 | protocol `event.rs` · server `core.rs` `client.rs` · client-wasm `core.rs` |
| gather wire v2 (hotbar select + weak mark) | `PROTO_VER` 2, goldens regenerated: every input frame carries `sel` in 3 bits (encode refuses 6+, decode refuses a forged 6–7) · new `KIND_EVENT` subtype weak-mark, swinger-only: cell + next mark heading (u8 over the yaw LUT) + weak-hit bit · client keeps one mark; harvest/respawn of its cell or a sync reset clears it (a mark announced in the join-reset tick is cleared by the reset batch and re-announced on the next landed hit — join-window noise, accepted) | protocol `lib.rs` `event.rs` · server `core.rs` · client-wasm `core.rs` `bridge.rs` |
| craft verb v0 | recipe rows bake from `content/recipes.toml` (station map none/workbench1/furnace · seconds 1..=65535, ×TICK_HZ into ticks · output count ≤ 255 · inputs ≤ 4) · per-player queue 4 jobs, dense, head at 0 · one request ≤ 99 units · inputs for the whole batch consumed at enqueue · one unit completes per recipe-ticks, paid via inv_add (full inventory ⇒ loss, announced with added 0 — same policy as gather until ground drops) · cancel by index refunds the remaining units' inputs (refund overflow lost too); an index naming no live job is ignored (a cancel racing a completion is a race, not an attack) · station-gated recipes need a placed station within the deployables v0 row's proximity radius at enqueue · integer refusal reasons 0–4: recipe/count/station/queue-full/inputs | sim-core `craft.rs` `limits.rs` · content `bake.rs` |
| build grid v0 | cell 3 m · storey 3 m · placement reach 5 m planar · levels 8 (wire-exact 3 bits) · grid 1024² cells (wire width; the island spans ~683) · foundation terrain: cell-center height ≥ 1.5 m and slope < 1.0 (the spawn walkability shape) · piece store 8 192, overflow refuses the placement · one plane (foundation L0 / floor / roof) + one riser (stairs) per (cell, level), one edge piece (wall/doorway) per boundary side, canonicalized west/north · support v0: foundation = terrain; wall/doorway = a foundation beside it at L0, else the edge below or a plane beside; floor/roof = an edge piece under any of its four sides; stairs = the plane they stand on · costs paid whole at placement, refusals are integer reasons 0–7 (7 = a foreign hearth's claim, deployables v0) · pieces carry hp + an upkeep clock and decay-remove (upkeep/decay v0) · pieces are movement collision and the store carries a derived column index (piece collision v0 row); piece damage and upgrade-in-place are the next slice | sim-core `build.rs` `limits.rs` |
| build wire v4 | `PROTO_VER` 4, goldens regenerated: C→S `KIND_ACTION` place (piece row 8 bits refused ≥ 32 · cell 10 + 10 bits · level 3 · loc 2) · S→C `KIND_EVENT` subtypes: piece-placed (broadcast to every client, not AOI'd — pieces are permanent world facts like slot changes) · piece-sync join walk batch 32 (store is append-only, so the walk is stable; broadcast/walk overlap dedups client-side by grid address) · build-refused reason · piece-def catalog batch 6 (shape/material/hp/costs — the build menu's data) · web: B toggles build mode, wheel cycles the piece, R/F the level, right-click places, one wireframe ghost — the plain-UI stand-in for the radial until the UI pass | protocol `lib.rs` `event.rs` · server `core.rs` `client.rs` · client-wasm `core.rs` `bridge.rs` · web `src/*` |
| deployables v0 | grid-addressed like pieces: body deploys at (cell, level, plane), doors in doorway edges · cost = 1 × the deployable's own item, consumed at placement · placement classes per schema: ground = level 0 buildable terrain with no plane piece · foundation = a plane piece at the level · any = either · doorway = a doorway edge piece · **hearth radius 24 m** planar from its cell center: foreign placement refuses (build reason 7 / deploy reason 7), no hearth inside any hearth's radius (own included, reason 8) · station proximity 5 m planar at craft enqueue (arms the workbench/furnace recipes; enqueue-time only) · **feed** moves ≤ 100 units/material/press into any hearth in reach, stock cap 2 000/material, no withdrawal until the container UI (stock readback rides the feed ack only) · deploy store 1 024 + hearth list 256, overflow refuses · deploy defs 16 (wire-exact 4 bits) · stock rows 4 (distinct build-cost items; bake refuses more) · bag cap 8 per owner (the ALPHA §1 cap; the 5 min cooldown lands with respawn-on-bag) · refusals are integer reasons 0–10 | sim-core `deploy.rs` `craft.rs` `limits.rs` |
| upkeep/decay v0 | period 1 h (108 000 ticks at 30 Hz) · per piece per period, each cost row charges ceil(cost × `upkeep_pct_per_day` / 100 / 24) — the pct is content (`balance.toml` globals, band-checked) — paid by the first hearth in list order within radius that can cover the whole charge · unpaid pieces decay 5% of max hp (min 1) per period; at 0 the piece is removed (broadcast; a removal restarts any in-progress sync walk) and the deployable at its exact address goes with it (the door in the doorway, the box on the floor) · deployables charge nothing: covered by any stocked hearth = free, uncovered = same decay curve (an empty hearth covers nothing, itself included) · no support cascade v0 — orphaned pieces just keep decaying if unpaid · sweep 64 entries per store per tick, catch-up ≤ 4 periods per visit (hours missed beyond that are forgiven; unreachable outside test clock-leaps) · placement stamps the current period | sim-core `deploy.rs` `build.rs` `limits.rs` |
| deploy wire v5 | `PROTO_VER` 5, goldens regenerated: C→S action subtype field 2 → 3 bits — deploy-place (row 4 bits, wire-exact · cell 10 + 10 · level 3 · loc 2) and feed (cell + level) · S→C event subtype field 4 → 5 bits, new subtypes: deploy-placed broadcast (address + row; owner stays sim-side until respawn-on-bag needs a lane) · deploy-sync join walk batch 24 · deploy-refused reason · deploy-def catalog batch 8 (arch/placement/hp/item) · piece-removed / deploy-removed broadcasts (address only) · stock ack (feeder-only: address + ≤ 4 rows of item + u32 units) · web: the build wheel extends past the piece table into the deployable rows, right-click places, E feeds the nearest mirrored hearth in reach, deployables render as per-archetype colored boxes (cosmetics row) | protocol `lib.rs` `event.rs` · server `core.rs` `client.rs` · client-wasm `core.rs` `bridge.rs` · web `src/*` |
| craft wire v3 (the action lane) | `PROTO_VER` 3, goldens regenerated: C→S `KIND_ACTION` rides the handshake bidi stream in the same ≤ 64 B frames as the hello (craft: recipe 8 bits refused ≥ 64, count 8 bits refused 0 or > 99 · cancel: index 3 bits refused ≥ 4); a malformed action frame drops the session (framing trust is gone) · per-conn action ring 8, overflow **backpressures** the stream reader — the reliable lane never drops · sim drains ≤ 1 action per client per tick into the command buffer; a full buffer defers to the next tick · S→C `KIND_EVENT` subtypes: craft-queue diff (dense live jobs + head-unit eta u16, shadowed per client like the inv diff; ev_resync forces a resend) · craft-done toast (item + added) · craft-refused reason · recipe-catalog drip (batch 4, craft time as raw ticks in 24 bits — no tick-rate coupling) · web panel: C toggles (exits pointer lock), click crafts, shift-click ×5, queue strip cancels on click, eta countdown re-anchors on every announce · client craft-toast ring 8 / refusal ring 4, drop oldest (cosmetic, like the gather toast ring) | protocol `lib.rs` `event.rs` · server `net.rs` `core.rs` `client.rs` · client-wasm `core.rs` `bridge.rs` · web `src/*` |
| piece collision v0 | capsule r 0.4 m · h 1.7 m (render capsule 0.4, eye 1.6) · piece vertical base = cell-center terrain sample + 0.3 m lift + 3 m/storey — the renderer's formula, now sim truth (lift, wall slab 0.24 m, doorway posts 0.9 m graduate from the cosmetics row) · planes (foundation/floor/roof) walk at their base; stairs are a ramp rising +Z through the storey · walls block their edge for the storey they span (capsule-height overlap; slab inflated by capsule r) · doorway blocks only its posts — the 1.2 m opening passes, the lintel can't matter at 1.7 m until a jump exists · a **closed door** in a doorway blocks the whole edge like a wall, an open one passes like an empty doorway (door v0 row) · stepping onto a built surface uses the 0.6 m step rule alone (the terrain cliff ratio is terrain's) · wade tests the effective ground, so a floor over shallows is dry · edge z-gate tests the endpoint, not the exact crossing (≤ 0.19 m steps: a fingertip of post corner, accepted) · column index: open-addressed 16 384 slots (2 × piece cap), linear probe, backward-shift delete, derived state — never hashed, rebuilt client-side when late piece-defs arrive (unknown rows collide as nothing until then; a reconcile absorbs it) | sim-core `collide.rs` `movement.rs` `limits.rs` · client-wasm `core.rs` `predict.rs` |
| door v0 + door wire v6 | a door places **closed** and seals its doorway; the **use** action toggles it, and any player within the 5 m build reach may — *superseded by the lock v0 row below: a door now places locked and answers only to its owner until its owner unlocks it; unlocked, this row's behavior is exactly what remains* · the sealed/open bit lives in the collision index beside the piece masks, kept in lockstep by `deploy.rs` on placement, toggle and removal, and re-derived client-side after anything that rebuilds that index · a use naming an address that holds no door refuses with reason 11 · `PROTO_VER` 6, goldens regenerated: C→S action subtype 5 = use (cell 10 + 10 · level 3 · loc 2 — address only, so two toggles racing can't fight over a state) · S→C event subtype 22 = door (address + absolute open bit, broadcast like a placement) · every placed-deployable record on the wire gained its open bit, so the join walk carries which doors stand open · your **own** door swings on the press, not on the reply (NETCODE.md §6.1): the client mirror and the predictor's shut bit flip optimistically, one prediction outstanding at a time so it never compounds, resolved by the next action outcome — the announcement is absolute so it confirms or corrects, and any deploy refusal rolls it back (refusals are sender-only, but with an 8-deep action ring the bounce may belong to an earlier queued action; an early rollback is harmless because a use that does land announces right behind it) · a rollback raises the deploy-applied flag, so the leaf the renderer swung on the press swings back — nothing else would ever tell it to, the sim's state having never moved · a sync walk re-derives regardless · web: E is the use key — the nearest door within **the sim's own 5 m cell-center reach** toggles, and only with none in reach does E fall through to feeding a hearth | sim-core `deploy.rs` `collide.rs` `build.rs` · protocol `lib.rs` `event.rs` · server `core.rs` · client-wasm `core.rs` `bridge.rs` · web `src/*` |
| lock v0 + lock wire v8 | a door places **locked to its placer** and only its owner may use it — every other hand bounces with reason 12 ("not your door"), which the placement/sync record and the door announcement let a client *see* coming (the locked bit is on the wire) without letting it *decide* (the owner id is not) · the **lock** action sets the bit absolutely rather than toggling, for the same reason the use action carries no state: two presses racing must agree on the result, not swap it · owner-only, build-reach-gated, and door-only, bouncing with the same reason 11 / reason 4 the use action does — the shared `door_in_reach` is what keeps the two verbs' refusals identical · locking never moves the leaf (a door locks open; its owner is then the one who can shut it) and one EV_DOOR carries open + locked together, absolute, so a client never holds half a door · `PROTO_VER` 8, goldens regenerated: C→S action subtype 6 = lock (address + 1 bit) · S→C event subtype 22 grew its locked bit · every placed-deployable record grew one too (31 bits now), so the join walk carries which doors stand locked · **no `mine` bit**: the client predicts its own press either way and a refusal rolls the leaf back through the door v0 path, which is DESIGN §5.6's own mispredict example — a foreign locked door flashes for one RTT and corrects, and the alternative (a per-recipient record) was judged not worth making a broadcast differ per viewer · **no shared access this slice** — one owner id, no codes, no crew list, matching the hearth claim's own owner-only rule; whether sharing arrives as a code lock, a hearth auth list that doors inherit, or crews is the open question, and it is the operator's to answer · no lock item: the lock is a property of the door, not a deployable, so nothing in `content/` moved · web: L locks or unlocks the nearest door in the sim's own 5 m cell-center reach, and a locked leaf renders banded iron (cosmetics row) | sim-core `deploy.rs` `world.rs` · protocol `lib.rs` `event.rs` · server `core.rs` · client-wasm `core.rs` `bridge.rs` · web `src/*` |
| upgrade v0 + upgrade wire v9 | a standing piece climbs the material ladder in place: same shape, same address, same collision, `content/building.toml`'s own words that a piece's `cost` is both its build cost and its **upgrade-into** cost — so wood → stone is priced at the stone row entire, and the ladder is a rung named, not a step taken (wood → metal in one press is legal wherever the table holds the row) · **damage carries as a fraction, never heals**: a wall at half hp becomes a stone wall at half hp (`old_hp × new_max / old_max`, floor, never below 1), so an upgrade is not a free repair mid-raid; the upkeep clock is left where it stood, an unpaid period still owed · sideways and downward refuse with reason 8 (`REFUSE_B_TIER`), as does a shape whose rung the table lacks · privilege is the placement rule unchanged — a foreign hearth's claim refuses (reason 7), and outside every claim anyone may upgrade anyone's wall, which is the same door the place verb already leaves open (pieces carry no owner; ownership is the hearth's job) · **no new S→C message**: an upgrade re-rows an address, which is exactly what the piece-placed broadcast says, and every mirror downstream already upserts by address — so the client's collision index swaps rows through the path the piece-defs drip already uses (NETCODE §6.2's structural log names `upgraded` as an event kind of its own; that is the chunk-versioned lane's shape, not today's flat broadcast — the kind splits out when that lane lands) · `PROTO_VER` 9, goldens regenerated and rekeyed `v9_*`: C→S action subtype 7 = upgrade (address + material 2 bits; three materials in two bits, so the fourth is forgeable and the decoder refuses it), the last code the 3-bit subtype field had left — subtype 8 costs a width bump · content gains a wall: the ladder must be whole (a stone rung with no wood one under it is a piece nothing can ever be upgraded into), refused at boot by `test_content` · no prediction rides along — an upgrade never moves collision, so there is nothing for the predictor to be wrong about · web: U climbs the nearest piece within the sim's own 5 m anchor reach one rung, and the tier color already reads the new material · gate coverage: `probe_parity` (bot 1 pokes the whole ladder every 16 ticks), `test_replay` (a scripted second wall reaches the stone rung and is refused both ways off it, asserted from the store), `test_alloc_zero` (a command arm of its own, over the builder bot's own cell so the paid write paths — insert, payment, re-row — are what the counter sees; two asserts there now refuse a run that only ever drove refusals), `upgrade_rides_the_wire`, `test_protocol_golden` + the forged-material domain test, `test_content`'s whole-ladder rule | sim-core `build.rs` `world.rs` `probe.rs` · protocol `lib.rs` · server `core.rs` · client-wasm `bridge.rs` · web `src/main.js` · content `validate.rs` |
| chat v0 + chat wire v10 | text on the bidi lane, two channels: **local** reaches everyone within `CHAT_LOCAL_CM` = 20 m planar of the speaker (the spoken §open "local chat" row, now a constant in `limits.rs`), **global** reaches every connected client — the speaker included either way, so the echo is the delivery receipt and a client never renders its own line on faith · **chat is not sim state**: no `Command`, no `World`, no WAL, so a replay reproduces a round without reproducing what anyone typed and determinism cannot depend on it (`test_chat_never_touches_the_state_hash` compares a chatting shard against a quiet one) · **the one player-authored payload**, so it is sanitized at both edges and the encoder refuses what the decoder would: trimmed of ASCII whitespace, non-empty, ≤ **48 UTF-8 bytes** (the cap the server's 64 B `read_frame` acceptance leaves room for), valid UTF-8, **no control characters** (a newline in a chat line is how one gets into a log), and a *non-canonical* line — one sanitize would have changed — is `Malformed` rather than repaired, so one line has exactly one encoding · profanity untouched (ALPHA §1) · **rate limit** per connection in the reader task: token bucket, burst 3, one token per 2 s (~30 lines/min sustained); refused lines are counted (`chat_rate_limited`), never announced — the limiter is the shard's business, not a conversation with the spammer · per-conn chat ring 4, **drop newest** counted, the opposite of the action ring's backpressure on purpose: chat is not a transaction, and stalling the shared stream on it would park a spammer's own build and craft lane behind their own typing · a chat frame that fails to decode is counted (`chat_bad`) and swallowed, where a malformed *action* still drops the session — chat text is the one payload a client bug can plausibly malform · sim drains one line per client per tick and the fan-out always takes it (said or lost, never deferred); a speaker the world has no body for is dropped and counted, and a recipient's full event ring loses the line without an `ev_resync`, because no sync walk would bring it back · `PROTO_VER` 10, goldens regenerated and rekeyed `v10_*`: C→S `KIND_CHAT` = 7 — **the last code the 3-bit kind field holds**, spent on chat because chat is text and not a sim command and the action lane's subtype space was full anyway; the next lane must subtype an existing kind, since widening `KIND_BITS` would widen every input datagram and every snapshot · S→C event subtype 23 = chat (speaker id 32 bits + global bit + the line) · client rings: wasm chat ring 16, drop oldest (deeper than the toast ring — a lost line is the one loss a player notices) · web: T or Enter opens a composer that owns the keyboard while open (no walking on a letter), Enter sends, Escape cancels, a leading `/g ` sends global, and the log sits bottom-left holding 8 lines that fade in 12 s unless the composer is held open; lines render through `textContent`, never `innerHTML`; the composer's `maxlength` counts UTF-16 units where the cap is UTF-8 bytes, so a refused line toasts rather than vanishing · gate coverage: `chat_wire.rs` (routing, the radius exact to the 3 cm quantum both sides, the state-hash comparison, the bodiless speaker), `test_protocol_golden` + `test_chat_text_domain_is_enforced` (both edges, forged frames both directions), `chat_limiter_spends_a_burst_then_drips`, `ci/client_smoke.mjs` (the raw C ABI both ways), `ci/browser_smoke.mjs` assertions 2 and 7 (typed into a real composer in one browser, read off the other's DOM — heard at the spawn, silent at 30 m+, global through) | protocol `chat.rs` `event.rs` · server `net.rs` `core.rs` `client.rs` `slot.rs` · sim-core `limits.rs` · client-wasm `core.rs` `bridge.rs` · web `src/*` |
| lighting v0 + the shadow gate | **One key, one fill, one bounded shadow map, one tone map** — the client had no shadow map at all and a 0.95 hemisphere doing all the work, so nothing cast, nothing received, and every object read as an untextured silhouette. **Key**: directional, azimuth 2.35 rad / elevation 0.36 rad (~21° — low, so everything rakes), colour `0xffe1b8`, intensity 3.0. **Fill**: hemisphere `0xa9c3e2` over `0x6b5f4a` at 1.15 — the whole ambient budget, and deliberately below the key (the gate asserts `key > fill`, because a fill that strong is the flat state this work ends). **Transfer**: `NeutralToneMapping` (Khronos PBR Neutral) at exposure 0.8, owned by the renderer, set on no material and followed by no post stack. Not ACES: its toe crushed the dark-albedo scatter (`0x2f6b33` pine) to near-black, which is a different bug from a dark scene — Neutral is identity below ~0.8 linear and only rolls off highlights, so the shadows this slice adds are the thing making the image dark. **Air**: fog `0x808f9c` 180–1000 m (was `0x8fb4d6` 250–900), and the sky is now a 24×16 back-face dome of radius 10 parked on the camera, vertex-coloured from the fog colour at the horizon to `0x2c4463` at the zenith on a `y^0.62` ramp. Geometry rather than a clear colour for one reason: fog is shaded and therefore tone-mapped, a clear colour is not, so painting the sky as geometry makes the horizon seam exact by construction instead of by tuning two numbers against each other. **Shadows**: `PCFSoftShadowMap`, 2048 px over an 80 m radius (texel 0.078 m), light parked 260 m back along the sun ray, ortho depth 1–520 m (relief is ~90 m, TERRAIN §6), depth bias 0 and normal bias **1.2 texels** (0.094 m) — in texels because the acne it fixes is a texel-footprint artefact. The box centre is snapped to its own texel grid **in light space** every frame (`threejs-shadow-systems`, clipmap ref §4) or the grid slides under the world and every silhouette shimmers; Z is quantized at 40 m because it moves depth coverage, not the projected grid. **The bound is real**: one map, so a caster more than 80 m from the player casts nothing. That is the one case the skill allows a single map, and the cascade/clipmap that would lift it is a later slice, not a claim made here. **Casters/receivers**: near ring both · far mesh receives only (it is the 8 m LOD of the ground the near ring already casts from — two disagreeing silhouettes in one map, for 131 k tris a frame) · scatter pools both · pieces, deployables, remotes both · water, sky dome, ghost and weak-mark neither. **`renderer.info.autoReset` is off** and `render()` resets it itself: three resets the counters *after* the shadow pass, so the defaults silently exclude every shadow draw — the exact half of the budget this rig adds. **Gate** (`ci/browser_smoke.mjs` assertions 9–11, all counted, none timed): the rig's flags; then the dev-only `shadowProbe` renders the live scene twice per yaw — key casting, then not — reads both frames back with `gl.readPixels` and requires the shadow pass to have darkened ≥ **1.5%** of the swept pixels by > **6/255** luma, over 4 yaws at pitch −0.45 from the pinned `dev_spawn` after a 4 s stream-in settle (pinned position on purpose: after the walk the frame depends on how much wall clock a shared box gave the input pump). Measured there: **9.39%** darkened, per-yaw 16.8 / 5.2 / 2.8 / 12.8, stable to the decimal across runs. Two more floors exist because the aggregate alone was **shown not to bite**: dropping `castShadow` from the terrain and the scatter pools still scored 6.12%, because the other tab's avatar stands on the shared spawn and under a 21° sun throws a long enough shadow to carry two of the four yaws on its own. So every yaw must clear **1%** (that mutation left two at exactly 0.0%), and the shadow pass must submit ≥ **8 draw calls** (it submitted 2 — the avatar's two meshes — against 25 intact, the seven `frustumCulled=false` scatter pools among them). The same two renders are what let the next assertion put DESIGN §9's budget under a gate for the first time: **58 draw calls, 483,550 triangles** both passes, against < 300 / < 1.5 M. Shadow map floor 1024 px. Probe is dev-only and the public-shard tab asserts its absence. | web `src/scene.js` `src/terrain.js` `src/main.js` · `ci/browser_smoke.mjs` · DESIGN §9 |
| materials v0 + the surface gate | **Four authored identities, one shared field, every channel from the same causes** — lighting v0 gave the world shape and it still had no *surface*: everything was `MeshLambertMaterial` at a flat colour, so nothing had roughness, nothing had detail, the ground was a per-vertex biome colour plus a ±0.035 per-vertex tint jitter, and the scatter pines were 0x2f6b33 repeated three hundred times. This is the splat material `TERRAIN.md` §4 always specified, built with **no textures at all**. **Identities** (the retired vertex palette's own colours, so the surface changed and the art direction did not): sand `0.78/0.71/0.52` rough 0.92 · grass `0.35/0.49/0.23` rough 0.82 · litter `0.15/0.33/0.16` rough 0.88 · rock `0.50/0.48/0.46` rough 0.70, with bump **strengths** 0.35/0.6/0.9/2.2 — unitless, because the two bump octaves are 5.6× apart in wavelength and one amplitude in metres cannot serve both (0.06 m over a 9.5 m wave is a slope of 0.006, which is a surface nobody can see; that mis-scaling was found by the probe below, at 1.2%). **Weights** ride the geometry as a 4-byte normalized `splat` attribute (4 B where the baked colour cost 12), written by the terrain worker from the sim's own (height, moisture, slope) as soft ramps **centred on `sim-core` `biome()`'s own hard edges**: beach 1–3 m (edge 2), alpine 44–60 m (edge 52), forest moisture 0.01–0.09 (edge 0.05), cliff 0.8–1.2 × tan 50° — a one-hot attribute would put a biome seam on the vertex grid that no in-shader noise can hide. **The field**: one hash-without-sine value noise, quintic, sampled three times per pixel at 1/48 · 1/9.5 · 1/1.7 m and shared by every channel (the skill's first failure mode is a PBR stack where each channel samples its own noise). It breaks the weights up by ±0.34 before a square-sharpen and renormalize, mottles albedo by ±0.16/0.11/0.07 per octave, varies roughness by ±0.18, and drives a surface-gradient bump (no tangents, no UVs — there are none) at **0.55 m and 0.09 m per octave**, both faded out by pixel footprint (meso 2→7 m, micro 0.3→1.1 m) so no normal survives below a pixel, then a specular-AA term at gain 0.5 widens the lobe by the perturbed normal's own variance. **Causal modifiers, each driving colour AND roughness together**: waterline wetness (−0.4→1.6 m, ×0.68 darker, roughness→0.28) which is what retired the separate sea-floor palette entry — 0.68 × sand lands on it — snow on high rock (52→80 m, to the retired `C_PEAK` `0.72/0.72/0.75`, roughness→0.55), and cliff darkening ×0.86 ramped over normal.y 0.78→0.60 (the sim's 50° threshold is 0.643). **Off the ground**: authored response bundles rather than colours — wood 0.95/0 · stone 0.88/0 · metal 0.42/**0.8** · foliage 0.86/0 · rock 0.85/0 · ore 0.55/**0.45** · cloth 0.78/0 · water **0.14**/0, so the build-tier read is sheen as much as colour (`bases.webp`) and a locked door wears the metal response with the iron colour. **Scatter** keeps one draw call per archetype and stops being one flat colour anyway: a merged trunk + skirt + crown pine with baked vertex-colour ramps (trunk 0x3a2a1e→0x53402d, skirt 0x1e4423→0x39733a, crown 0x2c5c2c→0x4d8845), plus a per-instance tint hashed from the slot's own cell (amplitude 0.17 pine / 0.19 bush / 0.10–0.12 nodes / 0.08 barrel, value and warm-cool on independent bits) — derived, never stored, so a chunk that streams out and back gets the same forest. **Gate** (`ci/browser_smoke.mjs` assertions 13–15, all counted, none timed): the structural facts (standard material, splat uniforms installed, four identities, ≥3 distinct roughnesses and ≥3 distinct bump strengths, three distinct tier roughnesses, metal a conductor and wood/stone not, water < 0.3, every scatter pool standard + vertex-coloured + instance-tinted with at least one metallic and at least one non-empty); then the **splat census**, ~106 k near-ring vertices read straight off the geometry, because a shader can blend four identities perfectly and still paint one if what it is fed is constant — measured spreads **1.00/1.00/0.00/0.16**, present 1.9/99.9/0.0/0.1%, deepest second identity **0.50**, **1.5%** of the ring a genuine blend (floors 2 channels ≥ 0.15 spread, 2 identities ≥ 1% present, second ≥ 0.35, mixed ≥ 0.5%; pinning the weights to a constant vector takes all four spreads to 0.000). Deliberately **not** an assertion about which biomes are in view: the moisture channel's features are ~700 m across, so a 320 m ring is usually one biome, and that is worldgen's fact, not the material's. Then the **surface probe**, the same 2N-frame shape as the shadow probe: the live scene rendered twice per yaw with `uSurface` at 1 and 0 — field on, field off, identity blend and causal modifiers identical in both — over 4 yaws at pitch −0.7 (steeper than the shadow probe's: this one is about the ground). Measured **12.81%** of 3,686,400 pixels moved by > 6/255 luma, per yaw 2.4 / 6.8 / 8.4 / 33.7, stable to the decimal. Floors 4% aggregate and 1% per yaw — and a third, because those two were **shown not to bite**: collapsing the noise scales so the whole ring lands in one lattice cell moved **66–96%** of the pixels and sailed past both, because a constant field is a uniform darkening. So every yaw must move pixels **both ways** ≥ 0.2% (that mutation scored +0.00% up on two of four yaws; the worst real yaw is +0.5%). Budget after: **58 draw calls, 492,118 triangles**, both passes, against < 300 / < 1.5 M. Both probes are dev-only and the public-shard tab asserts their absence alongside `shadowProbe`. | web `src/materials.js` `src/terrain.js` `src/terrainWorker.js` `src/scene.js` `src/main.js` · `ci/browser_smoke.mjs` · TERRAIN §4 |
