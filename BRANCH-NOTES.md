# Branch notes — `claude/next-files-cleanup-6t3ove`

**This branch carries no handoff.** It touched no crate, no content file and
no gate: the work was `NOW.md` itself, and the result is the file rather than
a note about it. Read `NOW.md`; there is nothing here it does not say better.

This file is **transient by protocol** (`gates-loop/GOAL.md` §the partial
rule): the loop's builder recreates it when it lands a partial slice, and it
describes whatever branch wrote it last. Read the heading before trusting a
word of it.

## What the previous note was, and why it is gone

It described `claude/building-placement-foundations-3gmwfk` — freehand
placement — which **merged as `a080787`** (PR #104). A merged branch's handoff
has been consumed, so keeping it here would have made a stale note look like a
live one for the second time in this file's history.

Two of its seven "what remains" items were **not** in `NOW.md` and would have
gone with it. Both are now `§0bl` items 5 and 6:

- **The stepped foundation, and do not widen the plate limits instead** —
  `reference/BUILDING.md` §7c.2 is a published, tested negative result on
  precisely that change. A negative result is the expensive kind of finding
  and the easy kind to lose.
- **The diagonal wall's √2 root scale stretches its UVs**
  (`render/structures.rs:1272`), pinned so it cannot grow.

Everything else it held is `NOW.md` §0bl items 1–4 and 7, or `§LOOK` item 11.
